# User Profile Form

Simple static user profile page to collect and persist:

- Contact information
- Address
- Credit card details
- Delivery instructions

The profile is stored in browser `localStorage` using key `user-profile-v1`.

## Run locally

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.
